[package]
name = "iris-native"
description = "Native fast path implementations for decoders iris already knows."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true
readme = "README.md"

[dependencies]
iris-abi.workspace = true
iris-format.workspace = true
iris-source.workspace = true
# For `RawBatch` and `Handshake`, which are the currency a native implementation deals in. Sharing
# those types rather than defining a parallel pair is what makes a differential run between a native
# kernel and the module it stands in for a comparison rather than a translation.
iris-vm.workspace = true
thiserror.workspace = true

[lints]
workspace = true
