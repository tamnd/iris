[package]
name = "iris-abi"
description = "The guest and host ABI for iris self-decoding datasets."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords.workspace = true
categories.workspace = true
readme.workspace = true

# Deliberately empty. iris-abi is compiled into every decoder, including decoders
# written by people who are not us in toolchains that are not Rust, so it has to be
# the smallest possible thing. `cargo tree -p iris-abi` showing nothing is a gate,
# not an accident. See the no_std job in CI.
[dependencies]

[lints]
workspace = true
